---
id: 1.2.0-W3-001
title: 盤點 LOG/UX/Testing 三維度設計缺口與改善計畫
type: ANA
status: in_progress
version: 1.2.0
wave: 3
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第二層:分析
  final_decision: W2 實機測試暴露設計缺口需系統性分析
  rationale: quality-baseline-rule-5
who:
  current: rosemary-project-manager
  history: {}
what: 盤點 LOG/UX/Testing 三維度設計缺口與改善計畫
when: W2 實機測試暴露設計缺口後
where:
  layer: 待定義
  files:
  - app/lib/
  - server/
  - app/test/
why: 實機測試發現企劃階段遺漏 LOG/UX/Testing 三維度，需系統性改善
how:
  task_type: Implementation
  strategy: 盤點現狀 → 識別缺口 → 產出改善方案 → 拆 IMP tickets
acceptance:
- '[ ] 確認 LOG 統一層設計方案'
- '[ ] 確認 UX 畫面狀態補全清單'
- '[ ] 確認 Testing 三層策略與 protocol integration test 方案'
- '[ ] 產出 IMP tickets 排入後續 Wave'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-19T20:21:43'
completed_at: null
created: '2026-06-19'
updated: '2026-06-19'
---

# Execution Log

## Task Summary

盤點 LOG/UX/Testing 三維度設計缺口與改善計畫

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 問題根因

W2 實機測試（W2-001~004）暴露企劃階段三大設計維度缺失：

1. **LOG**：D8 只規劃 server audit log，client-side 連線生命週期/protocol 訊息/認證結果零 log
2. **UX**：§1 操作盤點有失敗情境但未落地為畫面狀態機——每個畫面的狀態/操作/退出路徑未設計
3. **Testing**：D9 未區分測試層級——unit(mock) 全綠但 protocol integration test 不存在，192 測試遮蔽真實行為

### 影響範圍

| 維度 | 影響元件 | 嚴重度 |
|------|---------|-------|
| LOG | ConnectionManager, TerminalScreen, TtydProtocol, BiometricService, CredentialRepository, EnrollmentScreen | P1 |
| UX | TerminalScreen(idle/connected 缺 back/disconnect), 畫面狀態機未設計 | P1 |
| Testing | connection_flow_test(fake integration), protocol integration(不存在), widget test(不覆蓋導航) | P1 |

### 相關 Error Pattern

與 quality-baseline 規則 1 邊界相關：「測試綠燈不等於 Runtime 正確」（PC-165）

### 盤點數據

**LOG 現狀**：
- ConnectionManager: 10 個 developer.log（W2 hotfix 補的）
- TerminalScreen: 5 個 developer.log（W2 hotfix）
- TtydProtocol/BiometricService/CredentialRepository/EnrollmentScreen: 零 log
- Go proxy: slog JSON audit（僅連線事件），缺 WS upgrade 細節
- 無統一 logging 層，全部散落 developer.log

**UX 現狀**：
- HomeScreen: OK（root，有 Connect + Enroll）
- EnrollmentScreen/QrScannerScreen: OK（有 Navigator.pop）
- TerminalScreen idle: 缺 back
- TerminalScreen connected: 缺主動斷線/返回
- TerminalScreen error/disconnected/connecting: W2-001 已修 back

**Testing 現狀**（20 檔 ~192 test cases）：
- Unit(mock): 16 檔 ~184 cases，全用 Fake
- Integration(fake): 3 檔 ~22 cases，FakeWebSocketChannel
- Widget test: 3 檔 ~12 cases，不覆蓋 back/input
- Protocol integration(real): 0 檔 0 cases
- Go server integration_test.go: 存在（app 端不對稱）

## 重現實驗結果
### 實驗方法

以 rg/grep 掃描現有 log 點、navigation 呼叫、test case 覆蓋率，交叉比對 W2 tickets 暴露的具體問題。

### 實驗執行

1. rg 'developer\.log' app/lib/ → 15 處（全在 ConnectionManager + TerminalScreen，W2 hotfix）
2. rg 'context\.go\|Navigator' app/lib/ → 6 處導航呼叫，TerminalScreen 只有 error/disconnected 有 back
3. find app/test -name '*_test.dart' + rg 'test\(' → 20 檔 ~192 cases，0 個 protocol integration
4. Go server: slog 在 main.go:46，僅連線事件級

### 實驗發現

已驗證事實：
- Client-side LOG 是 W2 事後補的 hotfix，非設計產物（git diff e6cfb47..b3067b5 可驗證）
- TerminalScreen idle/connected 狀態仍缺 back/disconnect（code 可驗證）
- Protocol integration test 層完全不存在（app/test/ 目錄結構可驗證）
- 192 unit test 全用 FakeWebSocketChannel，無法偵測 text/binary frame 差異

未驗證假設：無（全部可由 code/git 驗證）

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->
### 改善方案

### 一、LOG 統一層設計

| 項目 | 方案 | 預期 IMP ticket |
|------|------|----------------|
| 建立 AppLogger wrapper | 封裝 developer.log，統一 name/level/格式 | IMP-1: LOG 統一層 |
| 連線生命週期 log 點 | connect/auth/handshake/data/disconnect 每步 | 同上 |
| Protocol log 點 | encodeInput/decodeOutput/buildAuthToken 進出 | 同上 |
| Auth/Credential log 點 | isAvailable/authenticate/load/save 結果 | 同上 |
| Enrollment log 點 | QR 掃描/解析/儲存 | 同上 |
| Go proxy log 補強 | WS upgrade 成功/失敗細節 | IMP-2: Server LOG 補強 |

### 二、UX 畫面狀態補全

| 畫面 | 缺失 | 修復 | 預期 IMP ticket |
|------|------|------|----------------|
| TerminalScreen idle | 無 back | 加 back 按鈕 | IMP-3: UX 導航補全 |
| TerminalScreen connected | 無斷線/返回 | 加 disconnect + back（需確認斷線後清理） | 同上 |
| 全畫面 | 無狀態機文件 | 產出畫面狀態矩陣文件（spec 補充） | DOC-1: 畫面狀態機 spec |

### 三、Testing 三層策略

| 層 | 現狀 | 改善 | 預期 IMP ticket |
|----|------|------|----------------|
| Protocol integration | 不存在 | 建立對真實 ttyd 的 WS 握手/auth/text frame 測試 | IMP-4: Protocol integration test |
| Widget test 補全 | 不覆蓋 back/input | 補 TerminalScreen back 按鈕 + text input + connected 狀態 | IMP-5: Widget test 補全 |
| CI 對稱 | Go 有 integration_test，app 沒有 | app 端 protocol test 納入 CI | 同 IMP-4 |

### Spawn 規劃

| 編號 | Type | 目標 | Priority |
|------|------|------|----------|
| IMP-1 | IMP | Flutter 端 LOG 統一層 + 全元件 log 點 | P1 |
| IMP-2 | IMP | Go proxy LOG 補強（WS upgrade 細節） | P2 |
| IMP-3 | IMP | UX 導航補全（idle/connected back + disconnect） | P1 |
| DOC-1 | DOC | 畫面狀態機 spec 文件 | P2 |
| IMP-4 | IMP | Protocol integration test（對真實 ttyd） | P1 |
| IMP-5 | IMP | Widget test 補全（back/input/connected 狀態） | P2 |

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
