---
id: 1.2.0-W1-003
title: 實作 bash-edit-guard bare cd 由 WARN 升級 DENY + heredoc/quoted FP 抑制（上游 PR）
type: IMP
status: pending
version: 1.2.0
wave: 1
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 框架 hook 防護升級（上游 PR）
  final_decision: 派發 basil-hook-architect 於 clone 改 hook + 測試 + PR
  rationale: Hook 程式碼+測試屬實作，PM 不自寫；hook 專家 basil 負責；框架變更走 PR review
who:
  current: basil-hook-architect
  history: {}
what: 於 tarrragon/claude 將 bash-edit-guard-hook.py 的 bare cd 分支由 permission_decision=allow（warn）改為
  deny（命令送出當下擋下，cwd 永不汙染）；補 heredoc body 與 quoted 字串內字面 cd 的 FP 抑制（避免擋掉合法 script 撰寫）；sed/perl
  原地編輯維持 warn 不變。補測試後開 PR
when: IMP-008 三度復發後根治
where:
  layer: 待定義
  files:
  - .claude/hooks/bash-edit-guard-hook.py
  - .claude/hooks/tests/test_bash_edit_guard_hook.py
why: warning-only 已三度被忽略（IMP-008）；設計上不需持久 cd 故 bare cd 永遠是錯誤，DENY 在源頭根除 cwd 汙染與其連環假性除錯
how:
  task_type: Implementation
  strategy: clone tarrragon/claude 新分支 → bare cd 分支改 deny + 補 heredoc/quoted 抑制 →
    既有測試不破 + 新增 FP 抑制與 deny 行為測試 → 全綠 → gh pr create
acceptance:
- '[ ] bare cd（行首/&&/;/'
- '[ ] ）回 deny 並附 git -C/子 shell 指引'
- '[ ] heredoc body 與 quoted 字串內字面 cd 不誤擋（FP 抑制有測試）'
- '[ ] 子 shell/git -C/uv -d/專案根還原 仍放行；sed/perl 維持 warn；既有測試全綠 + 新測試綠；PR 開於 tarrragon/claude'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: false
creation_accepted: false
started_at: null
completed_at: null
created: '2026-06-18'
updated: '2026-06-18'
---

# Execution Log

## Task Summary

於 tarrragon/claude 將 bash-edit-guard-hook.py 的 bare cd 分支由 permission_decision=allow（warn）改為 deny（命令送出當下擋下，cwd 永不汙染）；補 heredoc body 與 quoted 字串內字面 cd 的 FP 抑制（避免擋掉合法 script 撰寫）；sed/perl 原地編輯維持 warn 不變。補測試後開 PR

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->

### 工作環境（重要）

實作於 **tarrragon/claude 的 clone**：`/tmp/tcr-pr`，已在分支 `feat/bash-edit-guard-deny-bare-cd`（off origin/main，與 version-release PR 分支獨立）。
路徑無 `.claude/` 前綴——hook 在 `hooks/bash-edit-guard-hook.py`，測試在 `hooks/tests/test_bash_edit_guard_hook.py`。
**所有改動與 git/gh 操作都在 `/tmp/tcr-pr`，不要動 app_tunnel 本地。** 完成後 push 分支 + `gh pr create --repo tarrragon/claude`。

### 問題根因

`bash-edit-guard-hook.py` 的 bare cd 偵測已完整（正確排除子 shell / git -C / uv -d / 專案根還原），但行為是 `permission_decision="allow"` + warning。warning-only 已三度被忽略（IMP-008），cwd 汙染導致下游工具 `get_project_root()` 誤判、連環假性除錯。依設計（子 shell / git -C / uv -d / 絕對路徑）從不需持久 cd → bare cd 永遠是錯誤 → 應在執行前 DENY。

### 改動（檔案：`hooks/bash-edit-guard-hook.py`）

1. **bare cd 分支 allow → deny**：`main()` 中 `_find_bare_cd_target` 命中時，`emit_hook_output` 改 `permission_decision="deny"`，reason 含命中 target + 指引（改用 `git -C <abs>` / 子 shell `(cd ... && cmd)` / `uv -d <dir>`）。**sed/perl 原地編輯維持 warn（allow）不變**——僅 cd 分支改 deny；若同時命中 cd + edit，以 deny 為準。
2. **heredoc/quoted FP 抑制**（DENY 前必補，避免誤擋合法 script 撰寫）：`_find_bare_cd_target` 命中點若落在 heredoc body（`<<EOF ... EOF`）或單/雙引號字串內，視為字面 cd 不算 bare cd。實作建議：偵測 heredoc 區段範圍 + quote 狀態，命中點 offset 落在這些區段內則 skip（比照既有子 shell 括號深度的 offset 判定）。

### 測試要求（`hooks/tests/test_bash_edit_guard_hook.py`）

- 既有測試全綠（子 shell 排除 / git -C / 專案根還原 / 多行 / sed-perl warn）。
- 新增 deny 行為測試：bare cd（行首 / `&& cd` / `; cd` / `|| cd` / pushd）→ permission_decision == "deny"。
- 新增 FP 抑制測試：heredoc body 內 `cd /x`、單引號 `'cd /x'`、雙引號 `"cd /x"` → 不 deny（allow）。
- sed/perl 原地編輯仍 == warn（allow），未被 deny 波及。
- 跑測試：`uv run --project hooks pytest hooks/tests/test_bash_edit_guard_hook.py -v`；N>=1 全綠。

### 相關 Error Pattern

IMP-008（Bash 工作目錄污染，已記第三次復發 + 本 DENY 升級理由）；IMP-056（chpwd ls 淹沒）；tool-output-trust 規則 5。

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: basil-hook-architect
**Review Status**: pending
