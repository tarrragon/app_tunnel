---
id: 1.0.0-W1-003
title: Secret package 縮減：移除 proxy token 邏輯 + 適配 ttyd 帳密
type: IMP
status: in_progress
version: 1.0.0
wave: 1
priority: P1
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: fennel-go-developer
  history: {}
what: 保留 Load/Store/Config 機制，語意改為通用密鑰儲存；移除 proxy token 專屬預設值和註解；更新測試
when: v1.0.0 Wave 1（首批並行）
where:
  layer: 待定義
  files:
  - server/internal/secret/secret.go
  - server/internal/secret/secret_test.go
why: Tailscale 遷移：proxy 不再需要自有 token（SPEC-003 支援）
how:
  task_type: Implementation
  strategy: Phase 2 起：更新測試描述 → 移除 proxy token 預設值/註解 → 綠燈
acceptance:
- '[ ] Config/Load/Store 保留，語意改為通用密鑰儲存'
- '[ ] 移除 proxy token 專屬預設值/註解'
- '[ ] secret_test.go 更新測試描述'
- '[ ] go test ./... 100% pass'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-17T21:33:39'
completed_at: null
created: '2026-06-17'
updated: '2026-06-17'
---

# Execution Log

## Task Summary

保留 Load/Store/Config 機制，語意改為通用密鑰儲存；移除 proxy token 專屬預設值和註解；更新測試

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle

**遷移背景**：proxy 不再做認證，但 enroll 仍需儲存 ttyd 帳密。保留 package、更新語意。

**改動檔案**：`server/internal/secret/secret.go`, `server/internal/secret/secret_test.go`

**secret.go 改動清單**：
1. package 文件註解：「密鑰載入後端」→「通用憑證儲存後端」。移除 proxy token 字樣
2. 保留 Config/Load/Store API 不動
3. 更新所有函式註解：「密鑰」→「憑證」
4. Config struct Backend 說明改「憑證來源後端」

**secret_test.go 改動清單**：
1. 所有「密鑰」→「憑證」，「token」→「credential」
2. 保留測試邏輯不變
3. 確認有 file 後端 Load/Store round-trip 測試

**注意**：改動幅度最小（主要是註解語意更新）。不改 API 簽名。
**殘留檢查**：無 "proxy 密鑰"、"proxy token" 殘留

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: fennel-go-developer
**Review Status**: pending
