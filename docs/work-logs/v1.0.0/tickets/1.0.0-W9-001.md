---
id: 1.0.0-W9-001
title: 分析並修復 settings.local.json hook 幽靈/重複註冊的系統設計根因（單一註冊來源原則）
type: ANA
status: in_progress
version: 1.0.0
wave: 9
priority: P2
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path:
  entry_point: 第三層:設計問題立即修正
  final_decision: 固化 hook 註冊單一來源原則 + 強化 completeness-check 偵測 local 層 hook 註冊
  rationale: quality-baseline-rule-3 設計問題立即修正 + 規則 5 所有發現必須追蹤 + PC-053 framework 修改須有
    ticket
who:
  current: rosemary-project-manager
  history: {}
what: 分析並修復 settings.local.json hook 幽靈/重複註冊的系統設計根因（單一註冊來源原則）
when: sync-pull 後發現幽靈註冊 runtime 錯誤，根因分析後立即修正
where:
  layer: 待定義
  files:
  - /.claude/hooks/hook-completeness-check.py
  - /.claude/references/hook-system-operations.md
why: hook relocate 後 settings.local.json 殘留舊註冊形成幽靈，每次 Stop 觸發 runtime 錯誤；sync 排除 local
  檔無法自癒，需從設計層固化預防
how:
  task_type: Implementation
  strategy: 固化 hook 註冊單一來源原則（settings.local.json 禁止註冊 hook）+ 強化 completeness-check
    偵測 local 層任何 hook 註冊（latent ghost 預防），規則層 SSOT 先於 hook 強制層
acceptance:
- '[ ] ANA 含四根因分析（解耦無參照完整性／同步邊界盲區／無單一註冊來源／事後偵測非預防）'
- '[ ] 固化規則：settings.local.json 禁止註冊 hook（單一來源原則）'
- '[ ] hook-completeness-check 新增偵測：local 層含任何 hook 註冊即 WARNING（latent ghost 預防）'
- '[ ] 現有 settings.local.json 已清除 hook 註冊（前置已修）'
- '[ ] sync-push 完成'
tdd_phase: null
tdd_stage: []
assigned: true
creation_accepted: false
started_at: '2026-06-17T10:36:57'
completed_at: null
created: '2026-06-17'
updated: '2026-06-17'
---

# Execution Log

## Task Summary

分析並修復 settings.local.json hook 幽靈/重複註冊的系統設計根因（單一註冊來源原則）

---

## Problem Analysis
<!-- Schema[ANA/Problem Analysis]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

### 問題根因

（待填寫：問題發生的直接原因是什麼？）

### 影響範圍

（待填寫：哪些檔案、模組或功能受影響？）

### 相關 Error Pattern

（待填寫：是否有相關的已知錯誤模式？執行 /error-pattern query 確認）

<!-- 調查過程記錄（可選）：
搜尋指令：grep -rn "pattern" path/ --include="*.py"
確認的位置：
- file1.py:123
注意：接手者應獨立重新驗證數量/範圍（PC-162）
-->

---

## 重現實驗結果

### 觀測事實（固定值驗證，tool-output-trust-rules 規則 3）

| 檢查 | 命令 | 結果 |
|------|------|------|
| 舊路徑檔是否存在 | `ls .claude/hooks/handoff-auto-resume-stop-hook.py` | No such file or directory |
| 新路徑檔是否存在 | `ls .claude/skills/ticket/hooks/handoff-auto-resume-stop-hook.py` | 存在（35333 bytes） |
| settings.json 註冊路徑 | grep | `skills/ticket/hooks/...`（新，正確） |
| settings.local.json 註冊路徑 | grep | `.claude/hooks/...`（舊，失效） |
| settings.local.json 追蹤狀態 | `git ls-files` / `git check-ignore` | NOT tracked + gitignored（local-only） |

### 觸發鏈

1. hook 原位於 `.claude/hooks/`，早期被註冊進 `settings.local.json`（當時正確）。
2. 框架將 hook 模組化搬遷至 `.claude/skills/ticket/hooks/`，並更新 canonical `settings.json`。
3. `settings.local.json` 為 sync 排除檔，舊註冊未被任何機制清理 → 形成幽靈指向。
4. 每次 Stop 事件 runtime 嘗試執行舊路徑 → `No such file or directory`。

### 連帶觀測

同一 `settings.local.json` 另有 `agent-dispatch-validation-hook.py` 與 settings.json 重複註冊（PreToolUse:Agent），additive 合併語意下重複執行。

### 已驗證的既有防護缺口

`hook-completeness-check.py`（W9-004 / framework issue #2）已偵測 ghost + duplicate 並列於 SessionStart，但：(a) WARNING 不阻擋；(b) 淹沒在十餘條 SessionStart warning 中被略過；(c) 僅偵測「已成幽靈/重複」者，對「目前合法但 relocate 後必成幽靈」的 local 層 hook 註冊無預防。

## Solution
<!-- Schema[ANA/Solution]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[ANA/Test Results]: 選填（ANA 若有實驗輸出才填；無實驗可留 placeholder）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[ANA/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: rosemary-project-manager
**Review Status**: pending
