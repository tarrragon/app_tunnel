---
id: 1.0.0-W1-002
title: Enroll v2 遷移：移除 CF/token 欄位 + 更新 bundle 格式
type: IMP
status: in_progress
version: 1.0.0
wave: 1
priority: P0
parent_id: null
children: []
blockedBy: []
relatedTo: []
spawned_tickets: []
source_ticket: null
dispatch_reason: ''
decision_tree_path: null
who:
  current: fennel-go-developer
  history: {}
what: bundle struct 改為 v2（5 欄）；移除 CF Access + proxy token；移除 --reuse flag；更新 endpoint
  為 Tailscale 格式
when: v1.0.0 Wave 1（首批並行）
where:
  layer: 待定義
  files:
  - server/enroll.go
  - server/enroll_test.go
why: Tailscale 遷移：憑證包從 8 欄縮為 5 欄（SPEC-003 FR-01, FR-02）
how:
  task_type: Implementation
  strategy: Phase 2 起：寫 v2 bundle 測試 → 改 struct → 移除舊欄位 → 綠燈
acceptance:
- '[ ] bundle struct 改為 v2（5 欄：v, protocol, endpoint, ttyd_user, ttyd_pass）'
- '[ ] buildBundle 移除 CF Access + proxy token 參數'
- '[ ] runEnroll 移除 --reuse flag 和密鑰產生邏輯'
- '[ ] 測試覆蓋 v2 bundle 格式'
- '[ ] go test ./... 100% pass'
tdd_phase: phase1
tdd_stage:
- phase1
- phase2
- phase3a
- phase3b
- phase4
assigned: true
creation_accepted: false
started_at: '2026-06-17T21:33:39'
completed_at: null
created: '2026-06-17'
updated: '2026-06-17'
---

# Execution Log

## Task Summary

bundle struct 改為 v2（5 欄）；移除 CF Access + proxy token；移除 --reuse flag；更新 endpoint 為 Tailscale 格式

---

## Problem Analysis
<!-- Schema[IMP/Problem Analysis]: 選填（小型 IMP 可留 placeholder；大型 IMP 建議填寫）（.claude/pm-rules/ticket-body-schema.md） -->
### Context Bundle

**遷移背景**：Cloudflare Tunnel → Tailscale。憑證包從 v1（8 欄）縮為 v2（5 欄）。

**改動檔案**：`server/enroll.go`, `server/enroll_test.go`

**enroll.go 改動清單**：
1. `bundle` struct 改 v2（對齊 docs/contract.md）：V=2, Protocol, Endpoint, TtydUser, TtydPass（移除 CFAccessID/CFAccessSecret/ProxyToken）
2. `buildBundle` 只接收 endpoint, ttydUser, ttydPass
3. `runEnroll`：移除 cf-access-id/cf-access-secret/secret-backend/secret-file/keychain-service/keychain-account/reuse flag；移除 newToken() 和密鑰邏輯；移除 internal/secret import；endpoint 改 http://<tailscale-ip>:<port>/ws
4. 移除 `newToken` 函式、crypto/rand、encoding/hex import
5. 保留 `renderQR` 不變

**enroll_test.go 改動清單**：
1. 刪除 TestNewToken_Length64Hex、TestNewToken_Unique
2. 更新 TestBuildBundle：v2 格式（5 欄，V=2）
3. 新增 TestBuildBundle_V2Format（JSON 只有 5 key）、TestBuildBundle_Protocol

**殘留檢查**：無 CFAccessID/CFAccessSecret/ProxyToken/crypto/rand/encoding/hex

## Solution
<!-- Schema[IMP/Solution]: 選填（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## Test Results
<!-- Schema[IMP/Test Results]: 必填（至少記錄執行指令與通過數或 commit SHA）（.claude/pm-rules/ticket-body-schema.md） -->

<!-- To be filled by executing agent -->

---

## NeedsContext

<!-- 代理人回報資料缺口時追加於此（W17-010 協議）。子項建議 template：
- **缺失項**:（具體指出需要的 context 是什麼）
- **觸發位置**:（檔案:行號 或 決策點）
- **影響**:（缺料導致無法完成哪些 acceptance）
- **建議補料**:（PM 可採取的補充動作）
- **重派成本**:（若需重派所需 token/context 估算）
-->

---

## Exit Status

<!-- 代理人結束時以 YAML 格式回報（W17-010 schema）：
```yaml
status: success        # 枚舉: success|needs_context|blocked|partial_success|failed
reason: ""             # 狀態原因說明
confidence: 1.0        # 0.0-1.0 信心度
acceptance_met: []     # 已完成的 acceptance index 列表
acceptance_unmet: []   # 未完成的 acceptance index 列表
artifacts: []          # 產出檔案路徑列表
context_dependencies: []  # 依賴的 context 來源
estimated_recovery_effort: ""  # 若 needs_context/blocked，預估補料成本
```
對應 exit code: success=0, partial_success=0, needs_context=2, blocked=2, failed=1 -->

---

## Completion Info
<!-- Schema[IMP/Completion Info]: 必填（.claude/pm-rules/ticket-body-schema.md） -->

**Completion Time**: (pending)
**Executing Agent**: fennel-go-developer
**Review Status**: pending
